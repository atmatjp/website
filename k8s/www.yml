apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-website
  namespace: default
spec:
  revisionHistoryLimit: 2
  replicas: 1
  selector:
    matchLabels:
      app: astro-web
  template:
    metadata:
      labels:
        app: astro-web
    spec:
      containers:
      - name: website
        image: ghcr.io/atmatjp/website:391bc6b14abf1382c1398558e85396ef4081fde6
        ports:
        - containerPort: 80

      - name: cloudflared
        image: cloudflare/cloudflared:latest
        command: ["cloudflared", "tunnel", "--no-autoupdate", "run"]
        env:
        - name: TUNNEL_TOKEN
          valueFrom:
            secretKeyRef:
              name: cloudflared-secret
              key: TUNNEL_TOKEN
---
apiVersion: v1
kind: Service
metadata:
  name: web-service
  namespace: default
spec:
  selector:
    app: astro-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80